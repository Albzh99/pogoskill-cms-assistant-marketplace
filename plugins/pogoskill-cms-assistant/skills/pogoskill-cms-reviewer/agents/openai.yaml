interface:
  display_name: "PoGoskill CMS 审查 Agent"
  short_description: "独立检查 CMS 草稿字段、HTML、来源覆盖和图片"
  default_prompt: "Use $pogoskill-cms-reviewer to independently review this CMS draft through API readback and deterministic checks; do not open a page preview."
policy:
  allow_implicit_invocation: true
